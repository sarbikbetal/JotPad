<nav class="navbar navbar-expand-sm fixed-top navbar-light mb-3" style="background-color: #ffd05b; z-index:2">
    <div class="container">
        <button class="btn bmd-btn-icon sn-btn" type="button" data-toggle="drawer" data-target="#dw-s2">
            <span class="sr-only">Toggle drawer</span>
            <i class="material-icons">menu</i>
        </button>
        <a class="navbar-brand" href="/"><img src="/img/checklist.svg" width="30" height="30" class="d-inline-block align-top mr-2"
                alt="">Node JotPad</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav mr-auto">
                <a class="nav-item nav-link" href="/">Home</a>
                <a class="nav-item nav-link" href="/about">About</a>
            </div>
            <div class="navbar-nav">
                {{#if user}}
                <a class="nav-item nav-link" href="/todo">Notes</a>
                {{else}}
                <a href="/user/login"><button type="button" class="btn btn-outline-success mr-2">Login</button></a>
                <a href="/user/register"><button type="button" class="btn btn-outline-info">Register</button></a>
                {{/if}}
            </div>
        </div>

        <div class="dropdown pull-xs-right">

            {{#if user}}
            <button class="btn bmd-btn-icon dropdown-toggle" type="button" id="lr1" data-toggle="dropdown"
                aria-haspopup="true" aria-expanded="false">
                <i class="material-icons">account_circle</i>
            </button>
            {{else}}
            <button class="btn bmd-btn-icon dropdown-toggle sn-btn" type="button" id="lr1" data-toggle="dropdown"
                aria-haspopup="true" aria-expanded="false">
                <i class="material-icons">account_circle</i>
            </button>
            {{/if}}

            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="lr1">
                {{#if user}}
                <span class="dropdown-item"><i class="material-icons">account_box</i>{{user.name}}</span>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" data-toggle="modal" data-target="#logoutModal">Logout</a>
                {{else}}
                <a class="dropdown-item" href="/user/login">Login</a>
                <a class="dropdown-item" href="/user/register">Register</a>
                {{/if}}
            </div>
        </div>
    </div>
</nav>